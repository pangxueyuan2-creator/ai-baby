# 安全与隐私

目前维护 0.1.x。项目默认离线；第三方 provider 必须显式启用。用户资料和本地数据库没有应用层加密，请保护系统账户和备份。

发现安全问题时请使用仓库 Security 页的私密漏洞报告功能（若可用），不要在公开 Issue 中贴真实密钥、数据库、个人资料或原始敏感对话。若私密报告未开启，可提交不包含利用细节和敏感信息的联系请求。

请包含受影响版本、平台、虚构数据的最小重现步骤、预期 / 实际结果。我们不承诺固定响应时限。

模型回复和学到的文本都属于不可信内容。当前 provider 不具备工具执行、文件读取或数据库写入能力；不要为新 provider 引入隐式执行或自动上传。
