# 安全与隐私

目前维护 0.3.x，支持将 v1 / v2 数据库先创建一致性备份，再事务升级到 v3。迁移提交前和当前数据库启动时检查必要 schema、revision 和并发触发器；失败会保留数据并显示恢复说明，不会自动重置宝宝。

项目默认离线。仅填入密钥不会启用网络；外部 provider 必须同时显式选择并允许发送，不读取全局 `OPENAI_API_KEY`。在线上下文可能包含资料、近期聊天、相关记忆和明确标注的未确认候选。`store=false` 不能保证第三方不留存数据，取消或回滚也不能撤回已发送的请求。回环模型请求绕过系统代理，远程请求要求 HTTPS；不跟随重定向。

用户资料、候选、日记、导出和本地数据库没有应用层加密，请保护系统账户和备份。新数据库、备份和导出在 POSIX 上以 `0600` 独占创建，Windows 继承文件夹 ACL；不修改已有文件权限，也不防御拥有当前用户权限或控制数据目录的程序。备份含完整私人数据，导出含有效事实和重要经历，均不适合公开上传。

`/forget ID` 是逻辑遗忘：让指定事实（包括已被新值替代的旧版本）及关联 / 匹配经历退出检索，清理匹配追问、近期聊天、候选和旧日记。旧版含 `用户说：` 原文摘录的情绪经历缺少事实关联，且截断后无法可靠匹配，因此成功遗忘时全部失效，包括无关摘录；新版情绪经历不再复制原文。缓存回复正文被清空，保留窗口中的请求 ID 标记为撤销，旧请求重试不会重新学习。请求去重只覆盖最近 256 条身份；失效行、输入摘要、数据库空闲页以及已有备份仍可能保留内容。这不是安全擦除，也不是无限期阻止再次教学或对所有语义相关事实的删除。已发到外部服务的数据按其政策另行处理。

发现安全问题时请使用仓库 Security 页的私密漏洞报告功能（若可用），不要在公开 Issue 中贴真实密钥、数据库、个人资料或原始敏感对话。若私密报告未开启，可提交不包含利用细节和敏感信息的联系请求。

请包含受影响版本、平台、虚构数据的最小重现步骤、预期 / 实际结果。我们不承诺固定响应时限。

模型回复和学到的文本都属于不可信内容。内置 provider 接口不提供工具执行、文件读取或数据库连接，回复不会自动写成长期事实；不要为新 provider 引入隐式执行或自动上传。第三方 Python provider 是受信任的本地代码，并不处于沙箱中。上下文来源标注与提示词隔离不能保证模型不幻觉、误述回忆或服从注入指令。

日志和 CLI 错误仅显示脱敏类别，不输出 provider 错误正文、聊天全文或密钥。网络测试使用本地 HTTP 服务；维护测试和压力脚本只用合成资料与临时数据库，不调用付费 API。响应体大小、并发工作线程、重试和等待期限均有界；DNS、连接建立和 TLS 握手取消仍受系统限制，详见 [架构文档](docs/ARCHITECTURE.md)。
